spring:
  application:
    name: eureka-service
server:
  port: 8761
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      default-zone: http://${EUREKA_HOST:eureka-service}:${SERVER_HOST:server.port}/eureka


