server:
  port: 8081

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://${EUREKA_HOST}:8761/eureka
  instance:
    hostname: ${EUREKA_HOST}

logging:
  level:
    org:
      springframework:
        security: DEBUG