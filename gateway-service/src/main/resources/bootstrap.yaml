spring:
  application:
    name: gateway-service
  cloud:
    config:
      enabled: true
      uri: http://${CONFIG_SERVICE_HOST}:8000
logging:
  level:
    org:
      springframework:
        security: DEBUG
        cloud: DEBUG